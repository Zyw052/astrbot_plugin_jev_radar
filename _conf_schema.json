{
  "enable": {
    "description": "总开关",
    "type": "bool",
    "hint": "关闭后插件完全停用，不监听、不调用、不提醒。",
    "default": true
  },
  "api_key": {
    "description": "TypeSafe API Key",
    "type": "string",
    "hint": "在 https://console.typesafe.ai 获取。仅保存在本机 data/config 下，请勿泄露；留空则无法调用。",
    "obvious_hint": true,
    "default": ""
  },
  "base_url": {
    "description": "TypeSafe 接口地址",
    "type": "string",
    "hint": "默认 https://api.typesafe.ai，实际请求 POST {base_url}/v1/systemone。",
    "default": "https://api.typesafe.ai"
  },
  "model": {
    "description": "Jev 模型名",
    "type": "string",
    "hint": "默认 jev-latest；另有 jev-preview。",
    "default": "jev-latest"
  },
  "timeout_sec": {
    "description": "请求超时（秒）",
    "type": "int",
    "hint": "单次请求的超时时间，默认 20 秒。",
    "default": 20
  },
  "retries": {
    "description": "失败重试次数",
    "type": "int",
    "hint": "仅对 429/503/529（限流/过载）重试，默认 1 次；其它错误不重试。",
    "default": 1
  },
  "confidence_threshold": {
    "description": "置信度阈值",
    "type": "float",
    "hint": "意图置信度低于此值时标为「存疑」并在被动模式触发提醒，默认 0.75。",
    "default": 0.75
  },
  "risk_alert_threshold": {
    "description": "风险提醒阈值",
    "type": "int",
    "hint": "风险等级 0=安全 / 1=关注 / 2=高危；达到或超过此值触发提醒，默认 2。",
    "default": 2
  },
  "daily_call_limit": {
    "description": "每日调用上限",
    "type": "int",
    "hint": "费用保护：每日最多调用次数，0 表示不限，默认 2000。",
    "default": 2000
  },
  "passive_enabled": {
    "description": "启用被动模式",
    "type": "bool",
    "hint": "开启后自动监听消息判定意图，仅在低置信/高风险/关注意图时提醒。默认关闭，避免打扰与额外费用。",
    "default": false
  },
  "scope_group": {
    "description": "群聊生效",
    "type": "bool",
    "hint": "被动模式下是否处理群聊消息，默认开启。",
    "default": true
  },
  "scope_private": {
    "description": "私聊生效",
    "type": "bool",
    "hint": "被动模式下是否处理私聊消息，默认关闭。",
    "default": false
  },
  "group_whitelist": {
    "description": "群白名单",
    "type": "list",
    "hint": "填入群号，留空表示所有群均生效。仅被动模式使用。",
    "default": []
  },
  "watch_intents": {
    "description": "关注意图（命中即提醒）",
    "type": "list",
    "hint": "被动模式下，判定为这些意图时即使置信度高也会提醒。",
    "default": ["推销", "引战", "诈骗", "拉人"]
  },
  "desensitize": {
    "description": "发送前本地脱敏",
    "type": "bool",
    "hint": "开启后将手机号/邮箱/身份证/银行卡/超长数字替换为占位符再发送，默认开启。",
    "default": true
  },
  "max_chars_per_msg": {
    "description": "单条消息裁剪长度",
    "type": "int",
    "hint": "超过此长度的文本会被截断后再发送，默认 800。",
    "default": 800
  },
  "result_style": {
    "description": "结果展示样式",
    "type": "string",
    "options": ["详细", "简洁"],
    "hint": "详细=多行含置信度；简洁=单行摘要。",
    "default": "详细"
  },
  "log_decisions": {
    "description": "记录判定日志",
    "type": "bool",
    "hint": "开启后将判定结果写入 data/plugin_data/ 下的 decisions.jsonl（仅含消息哈希，不含原文），默认关闭。",
    "default": false
  },
  "reply_copilot_enabled": {
    "description": "启用回复副驾",
    "type": "bool",
    "hint": "回复副驾（Reply Copilot）总开关。默认关闭；开启后也仅按 trigger_mode 触发，绝不无差别推送。",
    "default": false
  },
  "trigger_mode": {
    "description": "副驾触发模式",
    "type": "string",
    "options": ["explicit", "risk"],
    "hint": "explicit=仅主人主动 /reply 指令触发（默认，最保守）；risk=仅 copilot_whitelist 白名单会话内，Jev 判定关注意图/高风险且置信度达标时才起草并推送给主人，原会话永无感知。",
    "default": "explicit"
  },
  "draft_count": {
    "description": "候选回复条数",
    "type": "int",
    "hint": "每次起草生成的候选回复条数（2~3），默认 3。",
    "default": 3
  },
  "draft_style": {
    "description": "回复风格",
    "type": "string",
    "options": ["concise", "polite", "firm"],
    "hint": "concise=简洁直接 / polite=礼貌得体 / firm=明确坚定，默认 concise。",
    "default": "concise"
  },
  "cooldown_minutes": {
    "description": "同会话起草冷却（分钟）",
    "type": "int",
    "hint": "risk 模式下同一会话两次 LLM 起草之间的最短间隔，默认 10 分钟；0 表示不冷却（不建议）。费用保护项。",
    "default": 10
  },
  "max_drafts_per_day": {
    "description": "每日起草上限",
    "type": "int",
    "hint": "费用保护：每日最多 LLM 起草次数（Jev 判定不占此额度，单独计数），0 表示不限，默认 20。",
    "default": 20
  },
  "notify_target": {
    "description": "副驾推送目标",
    "type": "string",
    "hint": "risk 模式下候选回复只推送给主人的会话标识（UMO，如 aiocqhttp:FriendMessage:xxxx）。留空则 risk 模式只记录不推送。副驾永不向原会话发送任何内容。",
    "default": ""
  },
  "copilot_whitelist": {
    "description": "副驾白名单（risk 模式）",
    "type": "list",
    "hint": "risk 模式仅在这些会话内生效（UMO 或群号）。留空=risk 模式完全不触发，最保守。",
    "default": []
  },
  "llm_provider_id": {
    "description": "起草用 LLM Provider",
    "type": "string",
    "hint": "留空=使用主人当前会话的默认 LLM Provider。可选填指定 provider id。",
    "default": ""
  }
}
